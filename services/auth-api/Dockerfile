# Auth Service (Spring Boot / Kotlin)
FROM eclipse-temurin:17-jdk-alpine
# Placeholder for build artifacts
# COPY build/libs/*.jar app.jar
# ENTRYPOINT ["java", "-jar", "/app.jar"]
CMD ["echo", "Auth Service Placeholder"]
